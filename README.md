# finalticket.netproject
A ticket management system built using C# ,ASP.NET Web API, and MySQL.The application enables users to create, view, and track support tickets, while admins can manage, assign, and update ticket status .the system follows layered architecture where desktop client communicate with the backend via RESTful APIs using JSON over HTTPensuring scalability
